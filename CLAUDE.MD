# 项目说明
love2d游戏引擎快速开发模板
- workspace: .
- platform: win10 android ios
- scm: git (已初始化)

----------------------------------------
## 一、权限范围（Permissions）
你正在操作当前项目目录及其所有子目录。
用户已完全信任此目录树，你对该目录及其子目录拥有完整权限。
你被授予以下读取权限：
```
https://github.com/love2d/love
```
### 1. 文件操作权限
- 读取当前目录及所有子目录中的任意文件
- 创建新文件或新文件夹
- 修改现有文件
- 删除文件或文件夹（如用户明确要求）
- 重命名文件或文件夹

### 2. Shell 命令权限
你可以直接执行以下命令（不限于此）：
- 文件类：ls、cat、rm、mv、cp、mkdir、rmdir
- 运行类：python、node、lua、go、bash、sh
- 工具类：grep、sed、awk、curl、wget
- 项目脚本：执行当前目录下的任意脚本

所有 Shell 命令均可直接执行，无需再次确认。

### 3. 工具内置命令和指令
你可以直接执行以下操作
- 工具前置指令
- 工具内置skills
- 工具内置agents

----------------------------------------
## 二、行为规范（Behavior Rules）

- 除非必要中文回复
- 始终假设你对整个目录树拥有持续访问权限。
- 当用户提出文件或目录操作请求时，直接执行，不需要额外确认。
- 当执行 Shell 命令时，直接返回命令输出。
- 如果路径或命令存在歧义，请向用户确认。
- 不得访问当前目录树之外的系统路径（如 C:\、/etc、/usr 等）。

----------------------------------------
## 三、Git 提交规范（Git Commit Rules）

本项目要求严格遵守 Git 提交规范。
你必须在每次完成一个需求后执行一次 Git 提交。
如果你是git子目录 你只需提交本目录下内容！

### 1. Git 初始化要求
如果当前目录尚未初始化 Git 仓库，你必须自动执行：`git init`，并创建一个初始提交（Initial commit）。
提交信息为：`Initial commit - Project initialized with full permissions`

### 2. 提交粒度要求
- 每完成一个明确的功能点、需求点、修复点，都必须进行一次 Git 提交。
- 不允许将多个无关修改合并到同一个提交中。

### 3. 提交信息格式（必须遵守）
提交信息必须使用以下格式之一：
```
feature: 描述新增的功能
fix: 描述修复的问题
refactor: 描述代码重构
docs: 描述文档更新
chore: 描述构建/脚本/依赖更新
test: 描述测试相关更新
```

### 4. 提交流程要求
每次完成需求后，你必须自动执行：
```
git add .
git commit -m "<符合规范的提交信息>"
```
----------------------------------------

## 四、自动加载文件（Auto Load）

在每次会话开始时，你必须自动读取目录下：

- `memory.md`（长期记忆, 自行维护，简洁明了）
- `feedback.md`（用户和你共同维护）
- `task.md`（开发记录，你维护）

并将其内容作为当前上下文的一部分。

----------------------------------------

## 五、工作流程规则（Workflow Rules）

### 1. feedback.md 更新规则
**每次代码修改完成后，必须更新 `feedback.md` 文档**

更新流程：
1. 完成代码修复/功能开发后，立即更新 `feedback.md`
2. 更新前必须先读取 `feedback.md` 和 `task.md`
3. 检查是否有新的任务需要继续处理
4. 如果有新任务，继续完成后再提交文档
5. 在 `feedback.md` 的"版本变更记录"表格中添加新记录
6. 在"详细记录"部分添加 BUG 或功能的详细说明

### 2. 任务连续性规则
- 每次会话必须检查 `task.md` 中的待办任务
- 如果有未完成的任务，优先继续处理
- 完成一个任务后再开始下一个任务
- 不允许跳过任务不做

----------------------------------------

## 六、文档更新规范

### 1. feedback.md 格式
- 使用表格记录版本变更
- 包含：状态、类型、编号、描述、优先级、日期
- 详细记录包含：问题描述、修复内容、影响范围

### 2. 提交时机
- 代码修改完成 → 更新 feedback.md → Git 提交
- 不允许只修改代码不更新文档

----------------------------------------

## 调试和日志

```
调试命令
love-11.5-win64\love.exe path\to\projectdir
日志（如果需要）
log\yyyy-mm-dd_HH_MM_SS.log 
```
